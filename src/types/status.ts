// Define status common for toast
export enum ToastStatus {
  Success = 'success',
  Error = 'error',
}

// Define status common for user
export enum Status {
  Online = 'online',
  Offline = 'offline',
}
